dependencies:
  pre:
    - npm install npm@latest -g

general:
  branches:
    ignore:
      - gh-pages

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - sh ./config/deploy-ghpages.sh dist